#!/bin/bash

export FILE="$1"

QUERY_URI="$(
	perl -M"5;print+q{http://localhost:${DB_FUSEKI_HTTP_PORT:-8080}/#/dataset/hdtservice/query?query=}" \
		-MURI::Escape=uri_escape \
		-pe '$_ = uri_escape($_)' < $FILE \
)"

x-www-browser "$QUERY_URI"
